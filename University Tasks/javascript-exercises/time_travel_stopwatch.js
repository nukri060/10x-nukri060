// Get the current year using new Date()
let currentYear = 2025;  

// Declare a variable for years jumped forward or backward
let yearsJumped = 5;  

// Calculate the new year after the time jump
let newYear = currentYear + yearsJumped;

// Log the new year
console.log(`Current Year: ${currentYear}`);
console.log(`After jumping ${yearsJumped} years, the new year is: ${newYear}`);
